plugins {
    id 'com.android.application'
}

// Version comes from CI (-Pvcode / -Pvname, from the run number & git tag).
// Computed as plain locals here to avoid a Groovy DSL parsing ambiguity that
// made `versionCode (…)` evaluate to null when written inline in defaultConfig.
def appVersionCode = (project.findProperty('vcode') ?: '1').toString().toInteger()
def appVersionName = (project.findProperty('vname') ?: '1.0.0').toString()

android {
    namespace 'xyz.dathevinci.twa'
    compileSdk 34

    defaultConfig {
        applicationId 'xyz.dathevinci.twa'
        minSdk 21
        targetSdk 34
        versionCode appVersionCode
        versionName appVersionName
    }

    signingConfigs {
        release {
            def ksPath = System.getenv('ANDROID_KEYSTORE_PATH')
            if (ksPath) {
                storeFile file(ksPath)
                storePassword System.getenv('ANDROID_KEYSTORE_PASSWORD')
                keyAlias System.getenv('ANDROID_KEY_ALIAS')
                keyPassword System.getenv('ANDROID_KEY_PASSWORD')
            }
        }
    }

    buildTypes {
        release {
            // Only attach the signing config when a keystore is actually present
            // (env set), so `assembleDebug` etc. still work without secrets.
            if (System.getenv('ANDROID_KEYSTORE_PATH')) {
                signingConfig signingConfigs.release
            }
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // The Trusted Web Activity launcher — no app code needed, the whole app is
    // this library's LauncherActivity pointed at the site (see AndroidManifest).
    implementation 'com.google.androidbrowserhelper:androidbrowserhelper:2.5.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
}
